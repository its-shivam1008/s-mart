/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','plus.unsplash.com', 'res.cloudinary.com', 'lh3.googleusercontent.com']
    }
};

export default nextConfig;
